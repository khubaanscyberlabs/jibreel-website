/* OLD PERFUME CARD STYLE - BACKUP START

.perfume-card {
    display: flex;
    flex-direction: column;
    background: #ffffff;
    height: 100%;

    border-radius: 16px;
    overflow: hidden;

    transition: all 0.35s ease;
    border: 1px solid rgba(0,0,0,0.05);
}

.perfume-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 35px rgba(8, 167, 197, 0.45);
}
.perfume-card-image-wrapper {
    aspect-ratio: 1 / 1;   
    background: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.perfume-card-image-wrapper img {
    height: 100%;
    width: 100%;
    object-fit: contain;
    padding: 10px 15px;
    object-position: center;
    transition: transform 0.4s ease;
}
.perfume-card:hover .perfume-card-image-wrapper img {
    transform: scale(1.08);
}

.perfume-card-body {
    padding: 10px 10px 12px;

    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
    text-align: center;
    height: 100%;
}

.perfume-card-name {
    font-family: 'Montserrat', sans-serif;
    font-size: 13px;
    min-height: 40px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: #121212;
    margin-bottom: 6px;
    line-height: 1.4;
    min-height: 36px;
}

.perfume-card-description {
    font-size: 12px;
    min-height: 38px;
    color: #1a1a1a;
    font-weight: 500;
    line-height: 1.3;

    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    min-height: 38px;
}
.perfume-rating{

color:#D4AF37;

font-size:13px;

margin-bottom:8px;

}

.perfume-card-btn {
    font-family: 'Assistant', sans-serif;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-top: 6px;
}

/* HOVER */

/* OLD PERFUME CARD STYLE - BACKUP START
.perfume-card-btn:hover {
    background: #023048;
    color: #08A7C5;
    border-color: #023048;
}


/* OLD PERFUME CARD STYLE - BACKUP START
.view-details {
    color: #023048;
    text-decoration: none;
    position: relative;
    margin-top: auto;
}

.view-details {
    display: inline-block;
    padding: 6px 14px;
    border-radius: 50px;
    background: #08A7C5;
    color: #023048;
    font-size: 12px;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.3s ease;
}

.view-details:hover {
    background: #023048;
    color: #08A7C5;
}

OLD PERFUME CARD STYLE - BACKUP END */

/* =============================================
SIZE SELECTOR
============================================= */

/* OLD PERFUME CARD STYLE - BACKUP START
.size-selector {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    width: 100%;
}

.size-btn {
    border: 1px solid #08A7C5;
    background: #08A7C5; /* feroza *
    color: #023048; /* carbon *
    padding: 6px 14px;
    font-size: 12px;
    border-radius: 50px; /* 🔥 sharp rectangle *
    cursor: pointer;
    transition: 0.3s;
}

/* ACTIVE STATE *
.size-btn.active {
    background: #023048;
    color: #08A7C5;
    border-color: #023048;
}

/* HOVER STATE *
.size-btn:hover {
    background: #023048;
    color: #08A7C5;
    border-color: #023048;
}

/* =============================================
QUANTITY SELECTOR
============================================= */

/* OLD PERFUME CARD STYLE - BACKUP START

.qty-minus, .qty-plus {
    border: 1px solid #08A7C5;
    background: #08A7C5;   /* feroza *
    color: #023048;        /* carbon *

    width: 26px;
    height: 24px;

    font-size: 14px;
    line-height: 1;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 12px;
    cursor: pointer;
    transition: all 0.25s ease;
}



/* HOVER — FINAL *
.qty-minus:hover, 
.qty-plus:hover {
    background-color: #023048 !important;  /* carbon *
    color: #08A7C5 !important;             /* feroza *
    border-color: #023048 !important;
}
/* PRESS EFFECT *
.qty-minus:active, 
.qty-plus:active {
    transform: scale(0.95);
}

/* VALUE *
.qty-value {
    min-width: 20px;
    text-align: center;
    font-weight: 500;
}
/* Responsive */

/* TABLET *
@media (max-width: 1024px) {
    .perfumes-grid {
        grid-template-columns: repeat(3, 1fr); /* 🔥 better use of space *
        gap: 16px;
    }
}

/* MOBILE *
@media (max-width: 768px) {
    .perfumes-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 14px;
    }
}

/* SMALL MOBILE (THIS IS WHAT YOU’RE MISSING) *
@media (max-width: 480px) {
    .perfumes-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 10px;
        padding: 0 10px;
    }
}
/* =============================================
   SECTION 4: ATTARS
============================================= */
/* OLD PERFUME CARD STYLE - BACKUP START
.attars-section {
    padding: 100px 5%;
   background: #f9fbfc;
    position: relative;
}

.attars-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 60%;
    height: 1px;
    background: linear-gradient(
        90deg,
        transparent,
       var(--gold),
        transparent
    );
}

.attars-header {
    text-align: center;
    margin-bottom: 70px;
}

.attars-subtitle {
    font-family: var(--font-ui);
    font-size: 0.7rem;
    letter-spacing: 6px;
    text-transform: uppercase;
    color: var(--feroza);
    margin-bottom: 20px;
}

.attars-title {
    font-family: var(--font-heading);
    font-size: clamp(2.2rem, 4vw, 3.2rem);
    font-weight: 400;
    letter-spacing: 2px;
}

.attars-divider {
    width: 70px;
    height: 1px;
    background: var(--feroza);
    margin: 25px auto 0;
}

.attars-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 30px;
}

.attar-card {
     background: #ffffff;
    border: 1px solid #e5e5e5;
    padding: 35px 25px;
    text-align: center;
    transition: var(--transition);
}

.attar-card:hover {
    transform: translateY(-6px);
    border-color: #023048;
    box-shadow: 0 15px 40px rgba(2, 48, 72, 0.15);
}

.attar-card-name {
    font-family: var(--font-heading);
    font-size: 1.3rem;
    margin-bottom: 10px;
    letter-spacing: 1px;
}

.attar-card-desc {
    font-size: 0.95rem;
    color: #121212;
    line-height: 1.6;
    margin-bottom: 15px;
}

.attar-card-size {
    font-family: var(--font-ui);
    font-size: 0.65rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--gray);
    margin-bottom: 20px;
}

.attar-card-btn {
    font-family: var(--font-ui);
    font-size: 0.6rem;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--feroza);
    border: 1px solid var(--feroza);
    padding: 12px 25px;
    display: inline-block;
    transition: var(--transition);
}

.attar-card-btn:hover {
    background: var(--feroza);
    color: var(--black);
}